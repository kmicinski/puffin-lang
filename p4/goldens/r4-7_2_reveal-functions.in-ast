((3) 0 () 0 () () (q program (define (main) (let ((board (read_board))) (let ((solution (solve_cell 0 0 board))) (lookup solution 8 8)))) (define (is_nil x) (eq? x (void))) (define (cons h t) (let ((c (make-vector 2))) (let ((_ (vector-set! c 0 h))) (let ((_ (vector-set! c 1 t))) c)))) (define (head c) (vector-ref c 0)) (define (tail c) (vector-ref c 1)) (define (make_cell r c v) (cons r (cons c (cons v (void))))) (define (cell_row cell) (head cell)) (define (cell_col cell) (head (tail cell))) (define (cell_val cell) (head (tail (tail cell)))) (define (block_index3 x) (if (< x 3) 0 (if (< x 6) 1 2))) (define (same_block? r1 c1 r2 c2) (if (eq? (block_index3 r1) (block_index3 r2)) (eq? (block_index3 c1) (block_index3 c2)) #f)) (define (lookup board row col) (if (is_nil board) 0 (let ((cell (head board))) (let ((r (cell_row cell))) (let ((c (cell_col cell))) (if (if (eq? r row) (eq? c col) #f) (cell_val cell) (lookup (tail board) row col))))))) (define (conflicts? board row col val) (if (is_nil board) #f (let ((cell (head board))) (let ((r (cell_row cell))) (let ((c (cell_col cell))) (let ((v (cell_val cell))) (if (if (eq? v val) (if (eq? r row) #t (if (eq? c col) #t (same_block? r c row col))) #f) #t (conflicts? (tail board) row col val)))))))) (define (solve_cell row col board) (if (eq? row 9) board (if (eq? col 9) (solve_cell (+ row 1) 0 board) (let ((existing (lookup board row col))) (if (eq? existing 0) (let ((candidate 1)) (let ((solution (void))) (let ((_ (while (if (< candidate 10) (eq? solution (void)) #f) (let ((_ (if (conflicts? board row col candidate) (set! solution solution) (let ((s (solve_cell row (+ col 1) (cons (make_cell row col candidate) board)))) (if (eq? s (void)) (set! solution solution) (set! solution s)))))) (set! candidate (+ candidate 1)))))) solution))) (solve_cell row (+ col 1) board)))))) (define (read_board) (let ((board (void))) (let ((i 0)) (let ((_ (while (< i 9) (let ((_ (let ((j 0)) (let ((_ (while (< j 9) (let ((_ (let ((v (read))) (if (eq? v 0) (set! board board) (set! board (cons (make_cell i j v) board)))))) (set! j (+ j 1)))))) (void))))) (set! i (+ i 1)))))) board))))))