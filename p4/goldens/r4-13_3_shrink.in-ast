((3) 0 () 0 () () (q program (define (mul x y) (if (<= y 0) 0 (+ x (mul x (- y 1))))) (define (divides? d n) (let ((r n)) (let ((_ (while (< 0 r) (set! r (- r d))))) (eq? r 0)))) (define (is_prime n) (if (< n 2) #f (let ((d 2)) (let ((prime #t)) (let ((_ (while (and prime (< d n)) (begin (if (divides? d n) (set! prime #f) (set! d (+ d 1))))))) prime))))) (define (count_primes n) (let ((cnt 0)) (let ((i 0)) (let ((_ (while (<= i n) (begin (if (is_prime i) (set! cnt (+ cnt 1)) (void)) (set! i (+ i 1)))))) cnt)))) (count_primes (read))))