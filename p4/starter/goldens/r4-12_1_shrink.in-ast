((3) 0 () 0 () () (q program (define (is_nil x) (eq? x (void))) (define (cons h t) (let ((c (make-vector 2))) (let ((_ (vector-set! c 0 h))) (let ((_ (vector-set! c 1 t))) c)))) (define (head c) (vector-ref c 0)) (define (tail c) (vector-ref c 1)) (define (mul x y) (if (<= y 0) 0 (+ x (mul x (- y 1))))) (define (read_list n) (let ((xs (void))) (let ((i 0)) (let ((_ (while (< i n) (begin (let ((v (read))) (set! xs (cons v xs))) (set! i (+ i 1)))))) xs)))) (define (eval_poly coeffs x) (if (is_nil coeffs) 0 (let ((rest (tail coeffs))) (let ((rec (eval_poly rest x))) (+ (head coeffs) (mul x rec)))))) (let* ((deg (read)) (n (+ deg 1)) (coeffs (read_list n)) (x (read))) (eval_poly coeffs x))))