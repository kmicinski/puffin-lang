((3) 0 () 0 () () (q program (define (main) (app (fun-ref count_primes) (read))) (define (mul x y) (if (if (< y 0) #t (eq? y 0)) 0 (+ x (app (fun-ref mul) x (+ y (- 1)))))) (define (divides? d n) (let ((r n)) (let ((_ (while (< 0 r) (set! r (+ r (- d)))))) (eq? r 0)))) (define (is_prime n) (if (< n 2) #f (let ((d 2)) (let ((prime #t)) (let ((_ (while (if prime (< d n) #f) (if (app (fun-ref divides?) d n) (set! prime #f) (set! d (+ d 1)))))) prime))))) (define (count_primes n) (let ((cnt 0)) (let ((i 0)) (let ((_ (while (if (< i n) #t (eq? i n)) (let ((_ (if (app (fun-ref is_prime) i) (set! cnt (+ cnt 1)) (void)))) (set! i (+ i 1)))))) cnt))))))