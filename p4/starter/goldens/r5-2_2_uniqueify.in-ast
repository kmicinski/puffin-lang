((3) 0 () 0 () () (q program (define (main) (count_evens_up_to (read))) (define (is_even n) (let ((_ (while (< 0 n) (set! n (+ n (- 2)))))) (eq? n 0))) (define (is_odd n) (not (is_even n))) (define (count_evens_up_to n) (let ((i 0)) (let ((cnt 0)) (let ((loop (void))) (let ((_ (set! loop (lambda () (if (if (< i n) #t (eq? i n)) (let ((_ (if (is_even i) (set! cnt (+ cnt 1)) (set! cnt cnt)))) (let ((_ (set! i (+ i 1)))) (loop))) cnt))))) (loop))))))))